device: 0
seed: 2021
log_interval: 300

# DATA
orig_dir: ../data/original_v3/
biom_dir: ../data/segmented/
spacy_dir: ../data/spacy/
pred_dir: ../predictions/
dev_miss: ../dev_miss.txt
output_folder: ../saved_models/
logs: ../logs/

exp_name: 'n2c2'
model_name: 'bert'

# Hyper-parameters
batch_size: 32
epochs: 10
warmup_epochs: 0


dummy_data: False
max_sen_len: 200
hidden_dim: 768 
# max_sent_len: 50
enc_dim: 768
enc_layers: 12
dropout: 0.3
lr: 0.00004
weight_decay: 0.000001
clip: 10
# max_vocab_size: 40000
# cutoff_freq:
# lowercase: True
patience: 5
accumulate_batches: 1
threshold: 0.5
event_weight: 0.5
verbs: False

# FLAGS
early_stop: True
# include_positions: True
freeze_pretrained: False
autoscalling: True

