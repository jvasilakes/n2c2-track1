You can view cue predictions using the `maskviewer.py` tool. For example,

```
python utils/maskviewer.py cue_detection/cue_predictions/Action.jsonl
```

This will run the tool in the browser at `localhost:5000`.
